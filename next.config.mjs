/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['plaid-merchant-logos.plaid.com', 'plaid-category-icons.plaid.com'],
    },
};

export default nextConfig;
